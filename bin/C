#!/bin/bash
SOCKS=

if pidof -x dispatch ; then
    SOCKS="--proxy-server=socks://localhost:1080"
fi

if [[ "$HOSTNAME" == "thinkpad1" ]] ; then
    OPTI=optirun
fi

exec $OPTI google-chrome --disable-session-crashed-bubble $SOCKS "$@"

