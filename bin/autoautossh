#!/bin/zsh
export AUTOSSH_LOGLEVEL=0 
export AUTOSSH_LOGFILE=/dev/null 

if ps aux | egrep '\<autossh' ; then
	echo "Bad News Bears"
	exit 0
else
    autossh -M0 -D 5190 -R 15014:localhost:22 -oBatchMode=yes -oExitOnForwardFailure=yes -T -N tunnel@justinhoppensteadt.com
fi
