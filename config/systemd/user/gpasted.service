[Unit]
Description=Gnome Clipboard Manager Daemon
After=awesome.service
BindsTo=graphical-session.target

[Service]
Type=idle
ExecStart=/usr/libexec/gpaste/gpaste-daemon

[Install]
WantedBy=graphical-session.target
